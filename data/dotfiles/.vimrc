"line numbers on
set nu

" indentation without hard tabs
set expandtab
set shiftwidth=4
set softtabstop=4

"set bracket match when text indicator is over them
set showmatch

"turn on syntax highlights
syntax on

"attempts to determine file type and to auto-indentying for filetime
filetype indent plugin on

" set nocompatible to ward off unexpected things that your distro might have made
set nocompatible
